# 开发规划 — 中文代码教学器

> 周期：3 天（纯前端无服务端）。适合单个 AI 一次做完，分四个阶段。

## Phase 1（4 小时）：静态页 + 讲解第一屏
- [ ] `index.html` 布局（输入区/输出区/年龄选择/按钮）
- [ ] `llm.js`：配置 key（可从界面填，存 localStorage）+ fetch 调用
- [ ] 输入示例代码 → 输出 `overview` 一行概述
- 验证：粘贴 `print("你好")` 能第一步出概述（手动 mock 也可）

## Phase 2（半天）：完整五步流程
- [ ] `ui.js` 五步卡片渲染（按序、每题先隐藏答案）
- [ ] 分区 JSON 契约实现（见开发规范 §3）+ 解析容错（返回畸形 JSON 时显示"重试"不白屏）
- [ ] "我没听懂"按钮 → 局部重讲
- 验证：20 行示例完整跑通五步，答案要先点击才显示

## Phase 3（4 小时）：年龄分级 + 挑战练习
- [ ] 四级词表模板生效（幼儿/小学/初中/成人）
- [ ] challenge 块可生成"改一个数字看效果"类任务（含 hint）
- [ ] localStorage 学习记录（只存标签）
- 验证：幼儿园模式讲解 `for i in range(3)` 用"三个人排队"；成人模式保留术语

## Phase 4（半天）：发布与打磨
- [ ] README（打开方式、key 配置、免责声明）
- [ ] 移动端适配（textarea 大字、按钮大）
- [ ] 可用 `server.py`（CORS + key 转发）或纯前端部署到 GitHub Pages
- [ ] 3 组演示用例预置（5/20/50 行）

## 验收清单
- [ ] 30 行代码 30 秒内出第一步概述
- [ ] 五步流程完整、答案先藏后揭
- [ ] 空输入/无 key/LLM 报错均不清屏崩溃
- [ ] 私密：任何情况下不向上传代码（本地跑预览确认 Requests 拦不到）

## 风险与对策
| 风险 | 对策 |
|---|---|
| CORS 拦截浏览器直调 API | 提供 server.py 小型转发 or 指导用户换成支持 CORS 的网关 |
| LLM 输出超时/达 100s | 30s 超时提示"再试一次"，可缩小篇幅 |
| 类比错误 | 每个类比标注"仅供参考"，答案查证建议 |