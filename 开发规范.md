# 开发规范 — 中文代码教学器

## 1. 总体约束
- **形态**：静态网页（`index.html` + `llm.js` + `ui.js`），本地双击即用；可选 `server.py`（轻量转发，防 CORS）
- **依赖**：零框架（手写 DOM）；LLM 调用走浏览器 fetch（或本地代理）
- **禁止**：任何代码可被上传到第三方（用户粘贴的代码只在浏览器内存/本地）

## 2. 界面与交互（硬性）
- 顶部：代码输入框（textarea）+ "讲解"按钮 + 年龄选择（幼儿园/小学生/初中/高中/成人）
- 输出区有五步流水，**按序渲染**（不是一次吐完）：
  1. `overview`：一句话概述
  2. `blocks`：逐块讲解（块 = 语义块，非一行）
  3. `terms`：关键词表（名称/含义/类比）
  4. `quiz`：3 道题（题干 + 隐藏答案，答完展开）
  5. `challenge`：改写练习（改一个量/条件，看会发生什么，+ 提示）
- 有"我没听懂"按钮 → re-request 用更简单词汇重讲该块（不重讲全部）

## 3. LLM 调用契约
- prompt 结构：`[角色]中文编程讲师，对象{年龄段}，词汇要求{限制}` + `[代码]` + `[输出要求]`
- 输出 JSON：
```
{"overview": "...", "blocks": [{"title": "...", "text": "..."}], "terms": [{"term": "...", "def": "...", "analogy": "..."}], "quiz": [{"q": "...", "a": "..."}], "challenge": {"task": "...", "hint": "..."}}
```
- age_level 映射词汇：
  - `kids`：≤ 100 基础词，类比（面包、排队、朋友）
  - `teen`：生活比喻，可提函数/循环
  - `adult`：正常术语但保留通俗讲解
- 流式渲染：LLM 一次返回即渲染为分步（不必全流）——保持响应快

## 4. 隐私与安全
- localStorage 只存"年龄偏好"与学习历史（代码本身**不存储**）
- 不发送 EXIF/路径/用户名；只发送代码 + 年龄分级
- 若本地代理（`server.py`），其自身不落盘

## 5. 界面规范
- 单页无路由；移动端可滚动；无登录
- 讲解卡片灰底白字?
（不用，用浅灰底；卡片四角圆角；重点词高亮）
- 每个 block 有"重新讲得更简单"按钮

## 6. 测试
- `tests/test_prompt.py`（mock LLM）：年龄分级词表正确映射、JSON 契约解析容错
- `tests/test_ui.js`（可选 vitest）：渲染函数五大步齐全、隐藏答案逻辑
- 人工验收：粘贴示例代码跑通 5 步，切换年龄分级

## 7. 交付物
- `index.html` 单文件可完整体验（含内联 CSS/JS 也可，但主体分文件便于维护）
- `README.md`（打开方式、key 配置、免责）
- 可选 `server.py` 转发 CORS 与 key 保护
- 3 组演示用例（5 行/20 行/50 行含报错）