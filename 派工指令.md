# 派工指令 — 中文代码教学器（CodeTutor CN）

> 依据「派工提示词 — 通用模板（v1）」填充。派发时连同本目录 思路.md / 开发规范.md / 开发规划.md / 架构设计.md 一并提供。
> 状态：**已完成开发并通过全部验收**（pytest 11 passed / node --test 28/28 / server.py 冒烟通过）。本文件用于日后复用、复派或审计。

---

【角色】你是独立软件工程师，负责从零实现一个工具，全程自主决策，不向用户提问。

【项目定位】
- 项目名：中文代码教学器（CodeTutor CN）
- 一句话描述：贴一段代码进去，它像老师一样逐步讲"这段代码在干什么、每个部分为什么这么写"，还能出练习题验你会没会。
- 所属目录：projects/05-中文代码教学器/
- 文档位置：
  1. 思路.md —— 产品背景、证据、边界与免责
  2. 开发规范.md —— 硬性规范（接口、安全、测试、交付物）
  3. 开发规划.md —— 分阶段计划与验收清单
  4. 架构设计.md —— 模块划分、接口契约、并行切分

【工作方式】
1. 开工前，先完整阅读上述 4 份文档，如有矛盾，以「架构设计.md > 开发规范.md > 开发规划.md > 思路.md」为优先级顺序。
2. 本派工为单 Agent 全量交付（开发规划.md 明确"适合单个 AI 一次做完"）：按「架构设计.md §5 并行切分表」实现全部模块，顺序为 prompts/storage 先行 → llm → state → ui 接入；**绝不修改**接口契约（JS 导出函数签名 / LessonData JSON schema / DOM 节点名 / localStorage key）。
3. 依次完成「开发规划.md」中的 Phase 1→2→3→4，每阶段末运行该阶段验证并记录结果。
4. 只使用文档中声明的依赖（零框架、手写 DOM、浏览器 fetch；测试可用 pytest）；新增任何第三方依赖前，先说明理由（文档未允许的一律不加）。
5. 全程不向用户提问；遇到歧义 → 选择文档规定的最保守/最安全方案，并在交付说明中标注。

【文档矛盾裁决（已按优先级裁定，直接照此执行）】
- 文件结构：开发规范.md 写 index.html+llm.js+ui.js；以架构设计.md §1 为准 → `index.html`、`ui.js`、`state.js`（核心状态机，LessonState）、`llm.js`、`prompts.js`、`storage.js`，依赖方向 `index.html → ui.js → state.js → llm.js/prompts.js/storage.js`；禁止 llm.js 读写 DOM、prompts.js 不 import 网络。
- quiz 契约：开发规范.md §3 写 `quiz:[{q,a}]`；以架构设计.md §2.2 LessonData 为准 → `quiz: {q, correct(0-2), options[3], explain}[]`，答案在用户回答前必须隐藏。
- 年龄分级：开发规范.md §3 是三级、界面是五标签；以架构设计.md §2.1 为准 → `AGE_LEVELS = {kids:0, child:1, teen:2, adult:3}` 四级；界面年龄选择提供"幼儿园/小学生/初中生/成人"四项（"高中"并入 adult），映射词汇规则以架构设计.md §2.3 AGE_RULES 为准。
- 测试：开发规范.md §6 指定 `tests/test_prompt.py`（pytest，mock LLM）；`tests/test_ui.js` 为可选——优先使用 node 内置测试器（node:test）实现，零第三方依赖。

【安全红线】
- 密钥/Token（API key）一律从界面输入并仅存 localStorage，绝不硬编码进任何源码文件、绝不写入日志
- 本工具对用户代码默认只读：粘贴的代码只在浏览器内存，任何情况下不写入 localStorage、不上传第三方（除用户配置的 LLM API 端点）；server.py 若实现则自身不落盘
- 只发送"代码 + 年龄分级"给 LLM API，不发送路径/文件名/EXIF 等

【代码规范】
- 语言/依赖/目录结构严格按「开发规范.md 第1节」：纯前端零框架；可选 `server.py`（轻量 CORS 转发）
- **实现注意（实测结论）**：file:// 协议下浏览器禁止 ES module，故各 JS 文件用 UMD 双模式（挂 `window.CodeTutor` + CommonJS `module.exports`），index.html 用普通 `<script>` 顺序加载，保证"本地双击即用"硬性约束；导出符号与契约签名不变
- 不加文档中未列明的注释（代码里维持最小注释，必要时只加文档字面要求）
- 所有公开函数按契约命名，docstring 只写用途一句话
- prompt 结构固定：`[角色]中文编程讲师，对象{年龄段}，词汇要求{限制}` + `[代码]` + `[输出要求]`，LLM_JSON_SCHEMA 内嵌进 prompt；禁用词用描述性禁止（不列出具体词），否则 kids 断言测试必挂

【测试要求】
- `tests/test_prompt.py`（pytest）：年龄分级词表映射、kids 模式 prompts 不含禁用词（断言）、JSON 契约字段、validateLesson 畸形数据容错——经 node 子进程执行**真实 JS** 断言
- `tests/test_llm.js` / `test_state.js` / `test_ui.js` / `test_storage.js`（node:test）：mock fetch / fake localStorage，不依赖网络
- 集成测试仅用于验证接口（页面加载、契约校验），不跑长链路
- 契约校验函数 `validateLesson(raw)` 对畸形 LLM 数据返回 `{ok:false, errors:[]}` 而非 throw；校验失败先自动重试一次（prompts 附纠错指令），仍败则显示友好错误卡片不白屏

【完成后交付内容（按此格式输出）】
1. 变更清单：实现了哪些模块，各阶段验证结果 + 截图/text 证据
2. 使用示例：至少 3 个命令/调用示例（含关键输出；含打开 index.html、配置 key、切换年龄分级的演示）
3. 部署/运行说明：打开方式（本地双击/静态服务器）、key 配置步骤、测试运行命令、可选 server.py 的启动方式
4. 与规范偏差表：若有偏离文档决策的项，列明与理由（尤其环境无 node 导致 vitest 跳过的说明）
5. 已知限制与本阶段未做的功能（按开发规划.md 勾选）

【接口核对清单（架构设计.md §6，验收关口，逐项给出结果）】
- [x] `validateLesson` 对畸形 LLM 数据返回 errors 而非 throw（tests/test_prompt.py + tests/test_llm.js 覆盖）
- [x] quiz 答案在 `answerQuiz` 前不外显 DOM（tests/test_ui.js 断言未作答不含 explain）
- [x] kids 模式 prompts 不含禁用词（tests/test_prompt.py 真实 buildSystemPrompt(0) 断言）
- [x] 无 API key/网络错误时 UI 显示可重试而非白屏（ui.js showError + btn-retry）

【验收清单】
- [x] `pytest tests/ -q` 全绿（11 passed）
- [x] `node --test "tests/test_*.js"` 全绿（28/28）
- [x] 交付示例逐条可运行（README.md 三方式打开 + server.py 冒烟 200/500 验证）
- [x] 没有未声明的网络调用（仅 llm.js 一个网络出口）
- [x] 无通过测试绕过（契约校验跑真实 JS）
- [x] README 已按开发规范.md §7 生成（打开方式、key 配置、免责声明）

---

## 实测交付摘要（2026-08 开发完成）

- **模块**：prompts.js / storage.js / llm.js / state.js / ui.js / index.html / server.py / examples(3) / tests(5) / README.md / package.json
- **与模板的已知偏差**：
  1. vitest → node:test（零第三方依赖，node v24 可用）
  2. ES module → UMD 双模式（file:// 硬性约束）
  3. kids 禁用词提示 → 描述性禁止（避免提示词自含禁用词）
- **已知限制**：真实 LLM 端到端联调未做（需用户提供 API key）；中途切换年龄段为整课重讲。
