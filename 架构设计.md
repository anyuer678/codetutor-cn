# 架构设计 — 中文代码教学器

> 目标：纯前端、单页、无后端；LLM 调用收敛单文件；教学流程用数据驱动（状态机步进）。

## 1. 总体分层

```
┌──────────────────────────────────────────┐
│          index.html（骨架/样式）         │
├──────────────┬───────────────────────────┤
│  ui.js       │   lesson.js（核心状态机） │
│  DOM渲染/交互 │  步骤推进/练答判定       │
├──────────────┼──────────────┬────────────┤
│  llm.js      │  prompts.js  │  storage.js│
│  API调用/契约│  年龄分级模板 │  localStorage
└──────────────┴──────────────┴────────────┘
```

依赖方向：`index.html → ui.js → state.js → llm.js/prompts.js/storage.js`。
**禁止**：`llm.js` 读写 DOM；`prompts.js` 不 import 网络。

## 2. 接口契约

### 2.1 `state.js`（核心，纯逻辑可单测）
```javascript
// === 领域模型 ===
const AGE_LEVELS = { kids:0, child:1, teen:2, adult:3 };

/**
 * 教学会话状态机
 * steps: ["idle","overview","blocks","terms","quiz","challenge","done"]
 */
export class LessonState {
  /**
   * @param {object} code         用户代码
   * @param {number} ageLevel     0-3
   * @param {(payload)=>void} onStep
   *                     回报事件: {type:"step", step, data}
   *                             {type:"reask", blockIndex}
   *                             {type:"done"}
   */
  constructor(code, ageLevel, onStep) {}
  start() {}            // 发起 llm.generateLesson()
  reexplain(blockIndex) // 只重讲某块（kids 词表）
  answerQuiz(answerIndex) // 记录并触发 reveal
  acceptChallenge()     // 公布参考解（要 todo 隐藏）
  setAgeLevel(level)    // 可中途切换重估词表
}

export function concatPrompt(code, ageLevel) // 由 prompts.js 输出
```

### 2.2 `llm.js`（网络唯一出口）
```javascript
/**
 * @typedef {Object} LessonData
 * @property {string} overview
 * @property {{title:string,text:string}[]} blocks
 * @property {{term:string,def:string,analogy:string}[]} terms
 * @property {{q:string,correct:number,options:string[],explain:string}[]} quiz
 * @property {{task:string,hint:string}} challenge
 */
export async function requestLesson(code, ageLevel, {
    apiBase, apiKey, model, signal,  } = {}) {
  // -> LessonData
  // 失败: throw LlmError({kind:"timeout"|"http"|"parse", detail})
}
export class LlmError extends Error { ... }

export const LLM_JSON_SCHEMA = `{
  overview: string;
  blocks: {title,text}[];
  terms: {term,def,analogy}[];
  quiz: {q,correct(0-2),options[3],explain}[];
  challenge: {task,hint}
}`; // 用于 prompt 内嵌规范期望
```

### 2.3 `prompts.js`（词表与角色卡）
```javascript
export const AGE_RULES = {
  0: { vocab: "仅限6岁词汇", ban: ["迭代器","异步","对象"], analogies: "游乐场/排队/朋友" },
  1: { vocab: "小学生能懂", ban: ["递归"], analogies: "生活比喻" },
  2: { vocab: "初中生", ban: [], allow: "函数、循环、数组" },
  3: { vocab: "成人但保持通俗", ban: [], allow: "全部术语" },
};
export function buildSystemPrompt(levelCode) {}
export function buildUserPrompt(code) {}
export function buildReexplainPrompt(blockTitle) {}  // "没听懂"重讲
```

### 2.4 `ui.js`（DOM 唯一出口）
```js
export function renderStep(stepName, data) {}    // 按序渲染六种卡片（overview/blocks/terms/quiz/challenge/done）
export function showError(e) {}
export function setBusy(bool) {}
export function mergeQuizPicks(data) {}          // quiz 事件数据合并用户所选（作答后选项标色）
export function renderIndicator(activeStep) {}  // 五步进度指示器 HTML（纯函数）
export function renderSkeleton() {}             // 加载骨架屏 HTML
export const DOM = { // 所有节点引用集中管理，避免散落字符串
  codeInput, ageSelect, btnGo, stepContainer,
  indicator, btnSample,
}
```
说明：`quiz`/`challenge` 步骤卡片在 state 重发同名事件时**替换**而非追加（作答后 / 接受挑战后），避免重复渲染。

### 2.5 `storage.js`
```js
export const pref = { getAgeLevel(), setAgeLevel(v), getHistory() }
// localStorage key: "codetutor.age"; 只存年龄与已完成课程名
// 不存代码本身；空实现可兼容隐私
```

## 3. UI 事件流（状态机）
```
用户粘贴代码 → go() → LessonState.start()
  → callLesson(Json) → 逐步 emit step
     overview → blocks(逐卡片) → terms → quiz(3题, 答案隐藏)
     → challenge → done
中途: "我没听懂"(on block) → reExplain → 该卡片重渲染 kids 化
完成后: localStorage.setHistory(课程摘要)
```

## 4. 契约校验（防 LLM 坏数据）
```js
export function validateLesson(raw) : {ok:boolean, data?:LessonData, errors:string[]}
```
- 必检：overview string；blocks[]非空；terms[]可空但object；quiz 每项 options.length===3 且 correct∈0..2；challenge 双字段
- 校验失败 → 一次重试（`prompts` 附纠错指令）→ 仍败则友好错误 Card

## 5. 并行开发切分
| 模块 | 依赖 |
|---|---|
| `prompts.js`+测试 | 无（纯文本） |
| `llm.js`+测试(mock fetch) | prompts |
| `state.js`+测试 | prompts+llm(mock) |
| `ui.js`+`index.html` | 全（先按 mock 数据开发） |
| `storage.js` | 无 |
顺序：prompts/storage 先行 → llm → state → ui 接入。

## 6. 接口核对清单
- [ ] `validateLesson` 对畸形 LLM 数据返回 errors 而非 throw
- [ ] quiz 答案在 `answerQuiz` 前不外显 DOM
- [ ] kids 模式 prompts 不含禁用词（断言测试）
- [ ] 无 API key/网络错误时 UI 显示可重试而非白屏